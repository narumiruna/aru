# User comment must survive targeted edits.
[project]
targets = ["codex", "claude"]

[skills]
"owner/repository" = { version = "0.5.0", include = ["writing-plans"], exclude = [

], paths = { writing-plans = "skills/writing-plans" } }

[mcp.docs]
transport = "streamable-http"
url = "https://docs.example.com/mcp"
bearer-token-env = "DOCS_TOKEN"

[mcp.docs.env-http-headers]
X-API-Key = "DOCS_API_KEY"

[mcp.demo]
command = "demo-mcp"
env-vars = ["DEMO_TOKEN"]
