# Manifest formats are intentionally unversioned during early development.
[project]
targets = ["codex"]

[skills]
"owner/repository" = { branch = "main", include = ["designing-user-experiences"], exclude = [

] }

[mcp]
