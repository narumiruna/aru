[package]
name = "aru"
version = "0.0.1"
edition = "2024"
description = "A project manager for coding-agent skills and MCP servers, written in Rust."
repository = "https://github.com/narumiruna/aru"
license = "MIT"
include = [
  "/Cargo.lock",
  "/Cargo.toml",
  "/LICENSE",
  "/README.md",
  "/docs/formats.md",
  "/docs/spikes/**",
  "/src/**",
  "/tests/cli.rs",
  "/tests/target_cli.rs",
]

[dependencies]
clap = { version = "4.5", features = ["derive"] }
fs2 = "0.4"
hex = "0.4"
inquire = { version = "0.9.4", default-features = false, features = [
  "crossterm",
  "fuzzy"
] }
percent-encoding = "2.3"
reqwest = { version = "0.12", default-features = false, features = [
  "blocking",
  "json",
  "rustls-tls"
] }
semver = { version = "1.0", features = ["serde"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
serde_yaml_ng = "0.10"
sha2 = "0.10"
tempfile = "3.20"
thiserror = "2.0"
toml = "0.9"
toml_edit = { version = "0.25.13+spec-1.1.0", features = ["serde"] }
url = "2.5"
walkdir = "2.5"

[dev-dependencies]
assert_cmd = "2.0"
predicates = "3.1"

[target."cfg(unix)".dev-dependencies]
expectrl = "0.9"
